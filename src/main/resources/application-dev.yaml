server:
  port: 8002
spring:
  application:
    name: JavaSpring
  datasource:
    url: jdbc:mysql//localhost:3336/shopdev
    username:
    password: root1234
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true